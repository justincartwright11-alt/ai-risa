# v2.5 Controlled Application Dry Runs Stabilization Validation Report

Date: 2026-04-01
Branch: v2.5-controlled-application-dry-runs
Scope: Stabilization only for slice 1 controlled application dry-run generator

## Stabilization command scope

1. Reran only `generate_model_adjustment_application_dry_runs.py`
2. Performed bounded deterministic validation checks against:
   - `ops/model_adjustments/controlled_model_adjustment_proposals.json`
   - `ops/model_adjustments/model_adjustment_approval_ledger.json`
   - `ops/model_adjustments/model_adjustment_validation_manifests.json`
   - `ops/model_adjustments/model_adjustment_application_gates.json`
   - `ops/model_adjustments/model_adjustment_application_packets.json`
   - `ops/model_adjustments/model_adjustment_application_dry_runs.json`

## Validation checks and outcomes

1. One-to-one proposal coverage: PASS
   - proposals=4
   - dry_runs=4
   - exact_once=True

2. Conservative rehearsal defaults: PASS
   - conservative_rehearsal_defaults_ok=True
   - verified defaults:
     - `dry_run_status = planned`
     - `execution_mode = simulation_only`
     - `dry_run_readiness = blocked`

3. Source path metadata correctness: PASS
   - source_paths_ok=True
   - expected canonical paths verified:
     - `ops/model_adjustments/controlled_model_adjustment_proposals.json`
     - `ops/model_adjustments/model_adjustment_approval_ledger.json`
     - `ops/model_adjustments/model_adjustment_validation_manifests.json`
     - `ops/model_adjustments/model_adjustment_application_gates.json`
     - `ops/model_adjustments/model_adjustment_application_packets.json`

4. Cross-layer linkage integrity: PASS
   - linkage_integrity_ok=True
   - each dry-run row links to matching proposal id, ledger entry id, manifest id, gate id, and packet id

5. Required dry-run field presence: PASS
   - step_checkpoint_field_presence_ok=True
   - each dry-run includes non-empty:
     - `ordered_dry_run_steps`
     - `checkpoint_sequence`
     - `required_preconditions`
     - `abort_conditions`
     - `expected_observations`

6. Deterministic ordering/coherence: PASS
   - deterministic_ordering_coherence_ok=True
   - dry-run proposal order matches source proposal order
   - `dry_run_id` sequence verified (`dry-run-001`, `dry-run-002`, ...)

## Conclusion

v2.5 slice 1 stabilization status: PASS

The controlled application dry-run layer remains read-only and non-executing. No proposal/ledger/manifest/gate/packet mutation, no auto-promotion to execution, and no scheduler/ingestion/resolver/model behavior changes were introduced during stabilization.