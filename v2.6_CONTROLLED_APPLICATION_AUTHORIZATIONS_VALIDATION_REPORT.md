# v2.6 Controlled Application Authorizations Stabilization Validation Report

Date: 2026-04-01
Branch: v2.6-controlled-application-authorizations
Scope: Stabilization only for slice 1 controlled application authorization generator

## Stabilization command scope

1. Reran only `generate_model_adjustment_application_authorizations.py`
2. Performed bounded deterministic validation checks against:
   - `ops/model_adjustments/controlled_model_adjustment_proposals.json`
   - `ops/model_adjustments/model_adjustment_approval_ledger.json`
   - `ops/model_adjustments/model_adjustment_validation_manifests.json`
   - `ops/model_adjustments/model_adjustment_application_gates.json`
   - `ops/model_adjustments/model_adjustment_application_packets.json`
   - `ops/model_adjustments/model_adjustment_application_dry_runs.json`
   - `ops/model_adjustments/model_adjustment_application_authorizations.json`

## Validation checks and outcomes

1. One-to-one proposal coverage: PASS
   - proposals=4
   - authorizations=4
   - exact_once=True

2. Conservative authorization defaults: PASS
   - conservative_authorization_defaults_ok=True
   - verified defaults:
     - `authorization_status = not_authorized`
     - `execution_authority = none`

3. Source path metadata correctness: PASS
   - source_paths_ok=True
   - expected canonical paths verified:
     - `ops/model_adjustments/controlled_model_adjustment_proposals.json`
     - `ops/model_adjustments/model_adjustment_approval_ledger.json`
     - `ops/model_adjustments/model_adjustment_validation_manifests.json`
     - `ops/model_adjustments/model_adjustment_application_gates.json`
     - `ops/model_adjustments/model_adjustment_application_packets.json`
     - `ops/model_adjustments/model_adjustment_application_dry_runs.json`

4. Cross-layer linkage integrity: PASS
   - linkage_integrity_ok=True
   - each authorization row links to matching proposal id, ledger entry id, manifest id, gate id, packet id, and dry-run id

5. Required authorization field presence: PASS
   - required_authorization_fields_ok=True
   - each authorization includes non-empty:
     - `required_signoffs`
     - `required_operator_role`
     - `authorization_window`
     - `final_pre_execution_conditions`

6. Deterministic ordering/coherence: PASS
   - deterministic_ordering_coherence_ok=True
   - authorization proposal order matches source proposal order
   - `authorization_id` sequence verified (`auth-001`, `auth-002`, ...)

## Conclusion

v2.6 slice 1 stabilization status: PASS

The controlled application authorization layer remains read-only governance-only output. No proposal/ledger/manifest/gate/packet/dry-run mutation, no auto-authorization, and no scheduler/ingestion/resolver/model behavior changes were introduced during stabilization.